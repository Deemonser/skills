interface:
  display_name: "Rust Build Governance"
  short_description: "Keep Cargo builds fast, scoped, and disk-bounded"
  default_prompt: "Use $govern-rust-builds to govern the Cargo commands this task will actually run without fragmenting caches."

policy:
  allow_implicit_invocation: true
