interface:
  display_name: "Proportionate Code Validation"
  short_description: "Match tests and checks to credible change risk"
  default_prompt: "Use $validate-code-proportionately to choose the narrowest sufficient validation for this change."
policy:
  allow_implicit_invocation: true
