interface:
  display_name: "Pick a Service"
  short_description: "Find the service ideas most worth validating"
  default_prompt: "Use $pick-a-service to screen these service opportunities and prepare the strongest candidates for validation."
policy:
  allow_implicit_invocation: true
