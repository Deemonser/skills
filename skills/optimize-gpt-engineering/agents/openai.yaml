interface:
  display_name: "GPT Engineering Optimizer"
  short_description: "Route engineering tasks to focused guidance"
  default_prompt: "Use $optimize-gpt-engineering to apply only the engineering guidance relevant to this task."
policy:
  allow_implicit_invocation: true
