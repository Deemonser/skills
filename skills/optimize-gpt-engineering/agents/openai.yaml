interface:
  display_name: "GPT Engineering Optimizer"
  short_description: "Apply a proactive, token-efficient GPT policy"
  default_prompt: "Use $optimize-gpt-engineering to directly apply the canonical proactive delegation, strict test-restraint, whole-system review, and risk-proportionate security template plus Codex worker defaults."
policy:
  allow_implicit_invocation: false
